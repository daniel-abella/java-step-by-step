/**
 * aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
 */
package cap01;

public /*aaaaaaaaaaaaaaaaa*/ class KeyWords {

	//Modificadores de acesso

	//public
	//private
	//protected
	//(default sem modificador)

	//Outros modificadores

	/*
	 final (torna a variável uma constante)
	 static (pertence à classe e não ao objeto)
	 abstract
	 	* em classe - não pode ser instanciada, só estendida
	 	* em métodos - não tem corpo
	 transient
	 	* atributos de classe - Não será serializado
	 strictfp
	 	* Usará o padrão de ponto flutuante, independente do SO
	 	  (Ieee...)
	 	* Aplicado a classes e métodos
	 synchronized
	 	* Só pode ser executado por uma thread por vez.
	 	* Aplicado a métodos
	 volatile
	 	* Uma espécie de sincronização para atributos
	 */

	//Tipos primitivos

	/*

	 byte  8 (-2^[BYTES-1] ATÉ (2^[BYTES-1])-1)
	 short 16
	 int   32
	 long  64
	 float 32
	 double 64
	 char 16 (SEM SINAL - UM CARACTER UNICODE)
	 boolean
	 void (tipo de retorno)
	 */

	//fluxo

	/*
	 if
	 else
	 for
	 while
	 do
	 break
	 continue
	 switch
	 case
	 default
	 return
	 */

	//OO

	/*
	 class
	 extends
	 implements
	 interface
	 super
	 this
	 new
	 package
	 imports
	 instanceof (erro do compilação caso as classes não tenha um parentesco vertical)
	 enum
	 */

	//Exceções
	/*
	 try
	 catch
	 finally
	 throw
	 throws
	 assert
	 */

	//Não usadas

	/*
	 goto
	 const
	 */

	//Não são, mas parecem

	/*
	 true
	 false
	 null
	 */

	private class Interna{


	}

	protected class Interna2{


	}

}


protected class MesmoArquivo{

}